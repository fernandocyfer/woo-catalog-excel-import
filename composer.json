{
	"name": "cyfer/wc-spreadsheet-product-importer",
	"description": "WooCommerce bulk product import: CSV and Excel (XLSX), simple and variable products, SKU-based upsert, admin UI with help models.",
	"version": "1.1.0",
	"type": "wordpress-plugin",
	"license": "GPL-2.0-or-later",
	"require": {
		"php": ">=8.0",
		"phpoffice/phpspreadsheet": "^2.1"
	},
	"config": {
		"sort-packages": true,
		"platform": {
			"php": "8.0.0"
		}
	},
	"autoload": {
		"psr-4": {
			"WCSPI\\": "app/"
		}
	}
}
